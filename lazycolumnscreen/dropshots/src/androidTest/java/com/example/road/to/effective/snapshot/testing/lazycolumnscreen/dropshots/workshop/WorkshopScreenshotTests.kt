package com.example.road.to.effective.snapshot.testing.lazycolumnscreen.dropshots.workshop

/**
 * Execute the command below to run only tests in this Workshop
 * 1. Record:
 *    ./gradlew :lazycolumnscreen:dropshots:connectedAndroidTest -Pandroid.testInstrumentationRunnerArguments.package=com.example.road.to.effective.snapshot.testing.lazycolumnscreen.dropshots.workshop -Pdropshots.record
 * 2. Verify:
 *    ./gradlew :lazycolumnscreen:dropshots:connectedAndroidTest -Pandroid.testInstrumentationRunnerArguments.package=com.example.road.to.effective.snapshot.testing.lazycolumnscreen.dropshots.workshop
 *
 * To run them using Android Orchestrator, add the following at the end of the command:
 * -PuseOrchestrator
 */
