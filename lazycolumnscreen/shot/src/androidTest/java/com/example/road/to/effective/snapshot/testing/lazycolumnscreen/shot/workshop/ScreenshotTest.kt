package com.example.road.to.effective.snapshot.testing.lazycolumnscreen.shot.workshop

/**
 * Record:
 * ./gradlew :lazycolumnscreen:shot:executeScreenshotTest -Precord
 *
 * Verify:
 * ./gradlew :lazycolumnscreen:shot:executeScreenshotTest
 *
 * Execute tests in this package only by adding the following to the previous command
 * -Pandroid.testInstrumentationRunnerArguments.package=com.example.road.to.effective.snapshot.testing.lazycolumnscreen.shot.workshop
 *
 * Record:
 * ./gradlew :lazycolumnscreen:shot:executeScreenshotTest -Precord -Pandroid.testInstrumentationRunnerArguments.package=com.example.road.to.effective.snapshot.testing.lazycolumnscreen.shot.workshop
 * Verify:
 * ./gradlew :lazycolumnscreen:shot:executeScreenshotTest -Pandroid.testInstrumentationRunnerArguments.package=com.example.road.to.effective.snapshot.testing.lazycolumnscreen.shot.workshop
 */
